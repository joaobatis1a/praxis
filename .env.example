# "mock" (default) uses the in-memory mock data. "supabase" wires the app to a real Supabase project.
VITE_DATA_SOURCE=mock

# Only needed when VITE_DATA_SOURCE=supabase — from Supabase dashboard: Project Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
